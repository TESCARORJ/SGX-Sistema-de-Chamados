spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chamados_geti
    username: chamados_geti_user
    password: chamadosgeti@001
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  ambiente: desenvolvimento

logging:
  level:
    br.geti.sistemachamado: DEBUG